Para rodar o sistema exporte as seguintes variaveis de ambiente.

```
export PGUSER='seu_user' \
PGPASSWORD='sua_senha' \
PGDATABASE='?'
PGHOST='?'
PGPORT='?'
```

sistema de testes de node com postgres seguindo o modelo do Vedovelli
